namespace os_ms_students.utils
{
    /// <summary>
    /// The Target Instance Scope to use when adding to the Service Collection
    /// </summary>
    public enum InstanceScope
    {
        Singleton,
        Scope,
        Transient
    }
}